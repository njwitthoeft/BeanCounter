##this module will handle the calibration and correction of values given a checkerboard image in the directory
getcalibration()
    #find checkerboard with opencv template matching or something similar
    #use checkerboard to calibrate camera
    #determine key values from this

calibrate_imgs(
    #use calibration factors from get_calibration to correct images
)